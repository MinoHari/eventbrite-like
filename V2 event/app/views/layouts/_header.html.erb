<nav class="navbar navbar-expand-lg navbar-dark shadow" style="background-color: #373634">
  <a class="navbar-brand" href="<%= home_path%>">Eventbrite Like⛵😇 </a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav ml-auto">
      <li class="nav-item">
        <a class="nav-link" href="<%= home_path %>">Accueil</a>
      </li>
      <%if session[:user] %>
      <li class="nav-item">
        <a class="nav-link" href="<%= new_event_path %>">Créer un évènement</a>
      </li>
      <%end%>
    </ul>
    <form class="form-inline">
      <%if session[:user] %>
      <a href="<%= logout_path(session[:user]['id'])%>" class="btn btn-outline-danger my-2 my-sm-0">Deconnexion</a>
      <%else%>
      <a href="<%= login_path%>" class="btn btn-outline-success my-2 my-sm-0">Se connecter ou s'inscrire?</a>
      <%end%>
    </form>
  </div>
</nav>